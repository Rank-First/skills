# Node / Playwright (installed next to the scripts by setup.sh).
# No trailing slash so it also ignores the node_modules SYMLINK setup.sh creates.
node_modules
package.json
package-lock.json

# Per-project resolved brand tokens + STYLE BLOCK (machine-local, not shared)
brand.local.md
**/reference/brand.local.md

# Generated images and Gemini's pre-encode JPEGs
*.raw.jpg
/tmp/
scratch/
